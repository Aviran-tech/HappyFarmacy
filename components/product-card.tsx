'use client'; import Link from 'next/link'; import {Star,Plus} from 'lucide-react'; import {useCart} from './cart'; import type {Product} from '@/lib/products'; export function ProductCard({product}:{product:Product}){const {add}=useCart();return <article className="group relative"><Link href={`/product/${product.id}`}><div className="relative aspect-[.92] overflow-hidden rounded-2xl bg-stone-200"><img src={product.image} alt={product.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/>{product.badge&&<span className="absolute left-3 top-3 rounded-full bg-cream px-3 py-1 text-[10px] font-bold uppercase tracking-wider">{product.badge}</span>}</div></Link><div className="pt-4"><div className="flex justify-between gap-3"><div><p className="text-xs uppercase tracking-widest text-moss">{product.category.replace('-',' ')}</p><Link href={`/product/${product.id}`} className="mt-1 block font-semibold">{product.name}</Link></div><button aria-label={`Add ${product.name} to cart`} onClick={()=>add(product)} className="mt-1 grid h-9 w-9 shrink-0 place-items-center rounded-full border border-forest transition hover:bg-forest hover:text-white"><Plus size={16}/></button></div><div className="mt-2 flex items-center justify-between"><span className="font-bold">₹{product.price}</span><span className="flex items-center gap-1 text-xs"><Star size={13} className="fill-honey text-honey"/>{product.rating.toFixed(1)}</span></div></div></article>}
