import {notFound} from 'next/navigation'; import {products,categories} from '@/lib/products'; import {ProductCard} from '@/components/product-card'; export default function Category({params}:{params:{category:string}}){const c=categories.find(x=>x[0]===params.category);if(!c)notFound();const items=products.filter(p=>p.category===params.category);return <main className="container pt-36 pb-24"><p className="eyebrow">Farm pantry / {c![1]}</p><h1 className="mt-2 text-5xl">{c![1]}</h1><p className="mt-4 max-w-lg text-lg text-ink/65">{c![2]}. Grown and gathered with a little more patience.</p><div className="mt-12 grid grid-cols-2 gap-x-4 gap-y-10 md:grid-cols-4">{items.map(p=><ProductCard product={p} key={p.id}/>)}</div></main>}
