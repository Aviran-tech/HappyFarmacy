import Link from 'next/link';export default function Account({params}:{params:{page:string}}){const isLogin=params.page==='login',isForgot=params.page==='forgot-password';if(params.page==='orders'||params.page==='profile')return <main className="container pt-36 pb-24"><p className="eyebrow">Your HappyFarmacy</p><h1 className="mt-2 text-5xl">{params.page==='orders'?'My orders':'My profile'}</h1><div className="mt-10 rounded-3xl bg-white p-10">{params.page==='orders'?'No orders yet — your next good thing is waiting.':'Sign in to view and update your profile.'}</div></main>;return <main className="grid min-h-[80vh] place-items-center px-6 pt-20"><form className="w-full max-w-md rounded-3xl bg-white p-8 shadow-soft"><p className="eyebrow">Welcome to the farm</p><h1 className="mt-3 text-4xl">{isForgot?'Reset your password':isLogin?'Welcome back':'Create your account'}</h1><p className="mt-3 text-sm text-ink/65">{isForgot?'We’ll send a reset link to your email.':'Your orders and favourites, all in one place.'}</p>{!isLogin&&!isForgot&&<input placeholder="Full name" className="mt-6 w-full rounded-xl border border-forest/15 p-3"/>}<input placeholder="Email address" className="mt-4 w-full rounded-xl border border-forest/15 p-3"/>{!isForgot&&<input type="password" placeholder="Password" className="mt-4 w-full rounded-xl border border-forest/15 p-3"/>}<button className="mt-5 w-full rounded-full bg-forest py-3 font-bold text-white">{isForgot?'Send reset link':isLogin?'Sign in':'Create account'}</button><div className="mt-5 flex justify-between text-xs underline"><Link href={isLogin?'/account/register':'/account/login'}>{isLogin?'Create an account':'Already a member?'}</Link>{isLogin&&<Link href="/account/forgot-password">Forgot password?</Link>}</div></form></main>}
