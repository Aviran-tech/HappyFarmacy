'use client'; import {useState} from 'react'; import {Minus,Plus} from 'lucide-react'; import {useCart} from '@/components/cart'; import type {Product} from '@/lib/products'; export function AddProduct({product}:{product:Product}){const[q,setQ]=useState(1),{add}=useCart();return <div className="mt-8 flex gap-3"><div className="flex items-center gap-4 rounded-full border border-forest/20 px-4"><button onClick={()=>setQ(Math.max(1,q-1))}><Minus size={15}/></button><span>{q}</span><button onClick={()=>setQ(q+1)}><Plus size={15}/></button></div><button onClick={()=>Array.from({length:q}).forEach(()=>add(product))} className="flex-1 rounded-full bg-forest py-4 font-bold text-white">Add to basket — ₹{p(product.price*q)}</button></div>} function p(n:number){return n.toLocaleString()}
