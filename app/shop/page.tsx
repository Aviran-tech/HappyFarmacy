'use client'; import {useMemo,useState} from 'react'; import {Search,SlidersHorizontal} from 'lucide-react'; import {products,categories} from '@/lib/products'; import {ProductCard} from '@/components/product-card'; export default function Shop(){const[q,setQ]=useState(''),[cat,setCat]=useState('all'),[price,setPrice]=useState('all'),[sort,setSort]=useState('featured');const shown=useMemo(()=>products.filter(p=>(cat==='all'||p.category===cat)&&(price==='all'||price==='under300'?p.price<300:price==='300to600'?p.price>=300&&p.price<=600:p.price>600)&&p.name.toLowerCase().includes(q.toLowerCase())).sort((a,b)=>sort==='low'?a.price-b.price:sort==='high'?b.price-a.price:b.rating-a.rating),[q,cat,price,sort]);return <main className="container pt-36 pb-24"><p className="eyebrow">The HappyFarmacy pantry</p><h1 className="mt-2 text-5xl">Shop all goodness</h1><div className="mt-10 flex flex-col gap-3 border-y border-forest/10 py-4 md:flex-row"><div className="flex flex-1 items-center gap-2 rounded-full bg-white px-4"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search the pantry" className="w-full bg-transparent py-3 outline-none"/></div><select aria-label="Category" value={cat} onChange={e=>setCat(e.target.value)} className="rounded-full border border-forest/15 bg-transparent px-4 py-3 text-sm"><option value="all">All categories</option>{categories.map(([id,n])=><option value={id} key={id}>{n}</option>)}</select><select aria-label="Price range" value={price} onChange={e=>setPrice(e.target.value)} className="rounded-full border border-forest/15 bg-transparent px-4 py-3 text-sm"><option value="all">Any price</option><option value="under300">Under ₹300</option><option value="300to600">₹300 – ₹600</option><option value="over600">Over ₹600</option></select><select aria-label="Sort products" value={sort} onChange={e=>setSort(e.target.value)} className="rounded-full border border-forest/15 bg-transparent px-4 py-3 text-sm"><option value="featured">Most loved</option><option value="low">Price: low to high</option><option value="high">Price: high to low</option></select></div><div className="mt-6 flex items-center justify-between text-sm"><span>{shown.length} products</span><span className="flex items-center gap-2 text-moss"><SlidersHorizontal size={15}/> Filters tailored for you</span></div><div className="mt-8 grid grid-cols-2 gap-x-4 gap-y-10 md:grid-cols-3 lg:grid-cols-4">{shown.map(p=><ProductCard key={p.id} product={p}/>)}</div>{!shown.length&&<div className="py-24 text-center"><h2 className="text-3xl">Nothing quite like that here.</h2><button onClick={()=>{setQ('');setCat('all');setPrice('all')}} className="mt-4 underline">Clear filters</button></div>}</main>}
